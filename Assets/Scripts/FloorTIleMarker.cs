using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// Marker component for environment effects.
/// Add this to any object that represents a floor surface
/// (sprite tiles, tilemaps, etc.) so RoomManager knows to apply
/// ice, lava, or other environment effects here.
public class FloorTileMarker : MonoBehaviour { }
